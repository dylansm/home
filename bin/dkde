#!/bin/bash

read -p "Are you sure you want to remove all containers and images? (y/n): " -n 2 -r
echo # cleaner prompt
if [[ ! $REPLY =~ ^[Yy]$ ]]
then
    exit 1
fi
# Delete all containers
docker rm $(docker ps -a -q)
# Delete all images
docker rmi $(docker images -q)
